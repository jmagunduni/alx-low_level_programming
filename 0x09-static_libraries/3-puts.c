#include <stdio.h>
/**
 * main - this is the main method of the program
 * Description: 'this is the hello world program which prints message
 * the message on the screen using the puts statement
 * Return:always 0 success 0 (Success)
 */
int main(void)
{
	/*this is the puts statement which printsto screen*/
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
